function Decoder(bytes, port) {
  var led = (bytes[0]);
  var sensor = (bytes[1]);
  var s_volt = ((bytes[2])/100);
  var ratio = ((bytes[3])/1);
  var rsr0 = ((bytes[4])/1);
  
  
  
    return {
      LED: led ,
      SENSOR: sensor,
      S_VOLT: s_volt,
      RATIO: ratio,
      RSR0: rsr0,
  }   
}